# Main 目录

## 功能说明

Main 目录包含游戏的主入口类，负责游戏引擎的初始化和启动。

## 主要文件

- **Main.java**: 游戏主入口类，负责创建窗口实例并启动游戏循环。

## 功能详解

1. **主入口方法** (`main`):
   - 启动游戏引擎
   - 设置全局异常处理器
   - 创建窗口实例
   - 启动游戏循环

2. **窗口管理**:
   - 提供静态方法 `getWindow()` 获取窗口实例
   - 管理游戏的生命周期

## 代码结构

```
Main/
└── Main.java       # 主入口类
```